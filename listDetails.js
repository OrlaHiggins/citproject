// // listDetails.js
// const mongoose = require("mongoose");

// const ListDetailsSchema = new mongoose.Schema({
//   userId: { type: mongoose.Schema.Types.ObjectId, ref: 'UserInfo' }, // Reference to the user who owns the list
//   items: [String] // Array of strings representing items in the list
// },
// {
//   collection: "Lists",
// });

// module.exports = mongoose.model('ListDetails', ListDetailsSchema);
